{
  "name": "fixing-everything-omarchy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:review": "node scripts/build.mjs --review",
    "dev": "vite",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "marked": "16.3.0",
    "vite": "7.1.5"
  }
}
